// We cannot end the filename extension with ts.
// We should use .js or no filename extension
import { add, sample } from "./utils/utils"; 

const sampleExample = sample([12,3,34]) // This is taken from the utils script
console.log(sampleExample)